# نظام إدارة طلبات المواد - مستند متطلبات المنتج

## 📋 المتطلبات الأصلية
نظام شامل لإدارة طلبات المواد وأوامر الشراء للشركات، يدعم أدوار متعددة (مسؤول النظام، مدير المشتريات، المهندسين، المشرفين).

## 👥 المستخدمون المستهدفون
- **مدير النظام (System Admin):** إدارة المستخدمين والإعدادات
- **المدير العام (GM):** الموافقة على أوامر الشراء
- **مدير المشتريات (Procurement Manager):** إدارة الموردين والطلبات وأوامر الشراء
- **المهندسون (Engineers):** تقديم طلبات المواد
- **مهندس الكميات (Quantity Engineer):** إدارة الكميات المخططة

## ✅ الميزات المكتملة

### يناير 2026
- [x] **إصلاح مشكلة الكميات الكسرية** (24 يناير 2026) - السماح بإدخال كميات كسرية مثل 0.25 و 1.5 في طلبات المواد
  - تعديل `MaterialRequestItem` model من `Integer` إلى `Float`
  - تعديل Pydantic schemas في `v2_requests_routes.py`
  - تعديل domain models في `app/requests/domain/models.py`
- [x] **نظام استيراد/تصدير متقدم للمشاريع** - تصدير واستيراد ملفات Excel متعددة الأوراق
- [x] **التحقق الصارم من الكتالوج عند الاستيراد** - جميع المواد المستوردة يجب أن تكون موجودة في كتالوج الأسعار
- [x] **ميزة "مزامنة مع الكتالوج"** - ربط المواد الموجودة مسبقاً بالكتالوج
- [x] **إصلاح خطأ 500 في إنشاء الطلبات** - حل مشكلة PostgreSQL `FOR UPDATE` مع aggregate functions
- [x] **تحسين البحث في الكتالوج** - عرض نتائج متعددة للبحث الجزئي

### يناير 2025
- [x] إعادة تصميم نافذة "إنشاء أمر شراء" بالكامل
- [x] إصلاح خطأ تكرار رمز الصنف عند تحويل RFQ إلى PO
- [x] إصلاح مشكلة فقدان البيانات (إضافة الملفات للـ .gitignore)
- [x] إضافة إعداد حد الموافقة للمدير العام
- [x] إصلاح زر "المشاريع" في القائمة الجوالية
- [x] إصلاح مشاكل CORS للنشر
- [x] **إصلاح خطأ التوقيت الزمني (Timezone Bug)** - استبدال `datetime.utcnow()` بـ `datetime.now(timezone.utc)`

### سابقاً
- [x] نظام المصادقة والتفويض
- [x] إدارة المستخدمين والأدوار
- [x] إدارة المشاريع
- [x] نظام طلبات المواد
- [x] نظام أوامر الشراء
- [x] نظام الموردين والكتالوج
- [x] نظام عروض الأسعار (RFQ)
- [x] نظام المباني
- [x] توليد ملفات PDF
- [x] النسخ الاحتياطي والاستعادة

## 📝 المهام القادمة

### P1 - أولوية عالية
- [ ] إعادة تصميم PDF عروض الأسعار (Quotation) ليطابق تصميم أمر الشراء (PO)

### P2 - أولوية متوسطة
- [ ] تحسين نظام النسخ الاحتياطي والاستعادة للبيانات المتداخلة
- [ ] استيراد معاملات المواد (factors) من Excel

### P3 - أولوية منخفضة
- [ ] ميزة التعيين الجماعي للمشرفين/المهندسين على المشاريع غير المعينة

## 🏗️ البنية التقنية

### Backend
- **Framework:** FastAPI (Python)
- **Database:** SQLite (تطوير) / PostgreSQL (إنتاج)
- **ORM:** SQLAlchemy (Async)
- **Authentication:** JWT

### Frontend
- **Framework:** React
- **UI:** Tailwind CSS + Shadcn/UI
- **State:** Context API

### البنية المجلدية
```
/app
├── backend/
│   ├── app/
│   │   ├── repositories/   # طبقة الوصول للبيانات
│   │   └── services/       # منطق الأعمال
│   ├── routes/             # API endpoints
│   └── database/           # نماذج قاعدة البيانات
└── frontend/
    └── src/
        ├── components/     # مكونات React
        ├── pages/          # صفحات التطبيق
        └── context/        # إدارة الحالة
```

## 🔑 بيانات الاختبار
- **Admin:** admin@system.com / password
- **Procurement:** procurement@test.com / password

## ⚠️ ملاحظات مهمة
- قاعدة بيانات الإنتاج على PostgreSQL تتطلب timezone-aware datetime
- الملفات `*.db` و `config.json` لا تُتبع في Git
- جميع URLs البيئة من ملفات `.env`
