# 📋 دليل نظام إدارة طلبات المواد (Talabat)
## الإصدار 2.2.0 - يناير 2026

---

## 🏗️ نظرة عامة على النظام

### الوصف
نظام متكامل لإدارة طلبات المواد والمشتريات للشركات الإنشائية. يشمل:
- إدارة طلبات المواد من المشرفين
- اعتماد الطلبات من المهندسين والمدراء
- إصدار أوامر الشراء
- تتبع التوريدات والاستلام
- نظام كميات العمائر (حساب الكميات بناءً على المساحات)
- نظام طلبات عروض الأسعار (RFQ)
- **نظام النسخ الاحتياطي التلقائي مع إدارة الإصدارات**

### الأدوار في النظام
| الدور | Role | الصلاحيات |
|-------|------|-----------|
| مشرف | supervisor | إنشاء طلبات المواد |
| مهندس | engineer | اعتماد/رفض الطلبات |
| مهندس كميات | quantity_engineer | مراجعة الكميات |
| مدير مشتريات | procurement_manager | إصدار أوامر الشراء |
| مدير عام | general_manager | اعتماد الأوامر الكبيرة |
| متتبع توريد | delivery_tracker | تسجيل الاستلام |
| طابعة | printer | طباعة الأوامر |
| مدير نظام | system_admin | إدارة النظام والنسخ الاحتياطي |

---

## 🖥️ البنية التقنية

### Stack التقني
```
Frontend: React 18 + Tailwind CSS + Shadcn/UI
Backend: FastAPI (Python 3.12) + SQLAlchemy (Async)
Database: PostgreSQL (Production) / SQLite (Development)
Authentication: JWT Tokens
```

### هيكل المشروع
```
/app
├── backend/
│   ├── server.py              # نقطة البدء
│   ├── database/
│   │   ├── models.py          # نماذج قاعدة البيانات
│   │   ├── connection.py      # اتصال PostgreSQL
│   │   ├── schema_version.py  # إدارة إصدارات المخطط ⭐ جديد
│   │   └── __init__.py        # تصدير النماذج
│   ├── routes/
│   │   ├── v2_auth_routes.py      # المصادقة
│   │   ├── v2_requests_routes.py  # طلبات المواد
│   │   ├── v2_orders_routes.py    # أوامر الشراء
│   │   ├── v2_delivery_routes.py  # التوريدات
│   │   ├── v2_buildings_routes.py # نظام العمائر
│   │   ├── v2_rfq_routes.py       # طلبات عروض الأسعار
│   │   ├── v2_system_routes.py    # إدارة النظام
│   │   └── v2_backup_routes.py    # النسخ الاحتياطي ⭐ جديد
│   ├── app/
│   │   ├── validators.py      # التحقق من المدخلات
│   │   ├── rate_limiter.py    # تحديد معدل الطلبات
│   │   ├── audit_logger.py    # سجل العمليات
│   │   └── backup_service.py  # خدمة النسخ الاحتياطي ⭐ جديد
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/             # صفحات التطبيق
│   │   ├── components/        # المكونات
│   │   │   └── ui/            # مكونات Shadcn
│   │   ├── context/           # AuthContext
│   │   ├── lib/               # أدوات مساعدة
│   │   └── utils/             # PDF Export وغيرها
│   ├── package.json
│   └── .env                   # REACT_APP_BACKEND_URL
└── memory/
    ├── PRD.md                 # متطلبات المنتج
    └── FORK_GUIDE.md          # هذا الملف
```

---

## 🗄️ نظام النسخ الاحتياطي (جديد في 2.2.0)

### الميزات الجديدة
1. **النسخ على مستوى قاعدة البيانات كاملة** - تلقائياً يشمل جميع الجداول
2. **إدارة إصدارات المخطط (Schema Versioning)** - كل تغيير هيكلي يُسجل برقم إصدار
3. **ربط النسخ الاحتياطية بالإصدار** - كل نسخة تحتوي على معلومات التوافق
4. **استرداد ذكي** - التحقق من توافق الإصدار قبل الاسترداد

### جداول إدارة الإصدارات
| الجدول | الوصف |
|--------|-------|
| schema_versions | سجل إصدارات المخطط |
| backup_metadata | بيانات النسخ الاحتياطية |

### APIs النسخ الاحتياطي
```
GET  /api/v2/backup/schema-info      # معلومات الإصدار الحالي
GET  /api/v2/backup/database-stats   # إحصائيات قاعدة البيانات
GET  /api/v2/backup/create-full      # إنشاء نسخة كاملة
POST /api/v2/backup/create-partial   # إنشاء نسخة جزئية
POST /api/v2/backup/validate         # التحقق من صحة ملف النسخة
POST /api/v2/backup/restore-full     # استرداد كامل
POST /api/v2/backup/restore-partial  # استرداد جزئي
GET  /api/v2/backup/history          # سجل النسخ السابقة
GET  /api/v2/backup/tables           # قائمة الجداول المتاحة
```

### استخدام النظام
```python
# إنشاء نسخة احتياطية من كود Python
from app.backup_service import BackupService

backup_service = BackupService(session)
result = await backup_service.create_full_backup(
    created_by="admin",
    notes="نسخة قبل التحديث"
)
print(f"Backup created: {result['backup_info']['backup_id']}")
```

---

## 🗄️ قاعدة البيانات

### الجداول الرئيسية

#### 1. جداول المستخدمين والمشاريع
| الجدول | الوصف |
|--------|-------|
| users | المستخدمين وصلاحياتهم |
| projects | المشاريع |
| suppliers | الموردين |

#### 2. جداول الطلبات والأوامر
| الجدول | الوصف |
|--------|-------|
| material_requests | طلبات المواد |
| material_request_items | أصناف الطلب |
| purchase_orders | أوامر الشراء |
| purchase_order_items | أصناف أمر الشراء |
| delivery_records | سجلات الاستلام |

#### 3. جداول نظام كميات العمائر (Buildings System)
| الجدول | الوصف |
|--------|-------|
| unit_templates | قوالب الوحدات (شقة، دور، إلخ) |
| unit_template_materials | مواد القالب |
| project_floors | أدوار المشروع ومساحاتها |
| project_area_materials | مواد المساحة (حديد، بلاط، إلخ) |
| supply_tracking | تتبع التوريد |
| buildings_permissions | صلاحيات نظام العمائر |

#### 4. جداول طلبات عروض الأسعار (RFQ)
| الجدول | الوصف |
|--------|-------|
| quotation_requests | طلبات عروض الأسعار |
| quotation_request_items | أصناف الطلب |
| quotation_request_suppliers | الموردين المطلوب منهم |
| supplier_quotations | عروض الموردين |
| supplier_quotation_items | أصناف العرض |

#### 5. جداول النظام ⭐ محدث
| الجدول | الوصف |
|--------|-------|
| price_catalog | كتالوج الأسعار |
| budget_categories | تصنيفات الميزانية |
| planned_quantities | الكميات المخططة |
| audit_logs | سجل العمليات |
| system_settings | إعدادات النظام |
| schema_versions | إصدارات المخطط ⭐ جديد |
| backup_metadata | بيانات النسخ الاحتياطية ⭐ جديد |

### العلاقات المهمة
```
MaterialRequest -> MaterialRequestItems (one-to-many)
PurchaseOrder -> PurchaseOrderItems (one-to-many)
PurchaseOrder -> DeliveryRecords (one-to-many)
Project -> ProjectFloors (one-to-many)
Project -> ProjectAreaMaterials (one-to-many)
UnitTemplate -> UnitTemplateMaterials (one-to-many)
QuotationRequest -> QuotationRequestItems (one-to-many)
QuotationRequest -> SupplierQuotations (one-to-many)
```

---

## 🚀 تحديث السيرفر (Production)

### معلومات السيرفر
```
IP: 13.235.247.19
User: ubuntu
App Path: ~/app
Service: talabat (systemd)
Database: PostgreSQL (talabat_db)
```

### أوامر التحديث الكاملة
```bash
# 1. الدخول للمجلد
cd ~/app

# 2. حفظ التغييرات المحلية (مثل .env)
git stash

# 3. سحب التحديثات
git pull https://github.com/notofall/finaltalabt.git

# 4. استعادة التغييرات المحلية
git stash pop

# 5. بناء الواجهة (إذا كان هناك تغييرات frontend)
cd frontend && yarn build

# 6. إعادة تشغيل الخدمات
sudo systemctl restart nginx
sudo systemctl restart talabat
```

### حل مشاكل Git الشائعة
```bash
# إذا ظهر خطأ "unmerged files"
git checkout --theirs frontend/.env
git add frontend/.env
git stash
git pull https://github.com/notofall/finaltalabt.git
git stash pop

# إذا أردت التخلي عن التغييرات المحلية
git reset --hard
git pull https://github.com/notofall/finaltalabt.git
```

### عرض سجلات الأخطاء
```bash
# سجلات الخدمة
sudo journalctl -u talabat --no-pager -n 50

# سجلات مفصلة
sudo journalctl -u talabat -f  # متابعة مباشرة
```

### ملفات .env المهمة
```bash
# Backend (.env) - لا تغيره إلا للضرورة
~/app/backend/.env
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@localhost:5432/talabat_db

# Frontend (.env)
~/app/frontend/.env
REACT_APP_BACKEND_URL=http://13.235.247.19
```

---

## ⚠️ المشاكل الشائعة وحلولها

### 1. خطأ "password authentication failed"
```bash
# السبب: كلمة مرور PostgreSQL خاطئة
# الحل:
sudo -u postgres psql
ALTER USER postgres WITH PASSWORD 'newpassword';
\q

# ثم حدث backend/.env
```

### 2. خطأ "database does not exist"
```bash
# الحل:
sudo -u postgres psql -c "CREATE DATABASE talabat_db;"
```

### 3. خطأ 422 عند جلب الكتالوج
```
السبب: limit يتجاوز MAX_PAGE_SIZE (100)
الحل: تغيير limit في Frontend من 500 إلى 100
```

### 4. أمر الشراء لا يذهب للمدير العام
```
السبب: الحالة تُضبط على "pending" بدلاً من "pending_gm_approval"
الملفات المتأثرة:
- backend/routes/v2_orders_routes.py
- backend/app/services/rfq_service.py
```

### 5. فشل استيراد المشروع
```
السبب المحتمل:
1. لم يتم تحديث السيرفر (git pull)
2. أعمدة مفقودة (project_name, created_by)
الحل: التأكد من تنفيذ git pull وإعادة تشغيل الخدمة
```

### 6. مشكلة ObjectId في MongoDB (غير مستخدم حالياً)
```
السبب: ObjectId غير قابل للتحويل لـ JSON
الحل: استخدام {"_id": 0} في projection أو تحويله لـ string
```

---

## ✅ التحسينات المُنفذة (يناير 2026)

### تحسينات الوظائف
- [x] دعم الكميات الكسرية (0.25، 0.5)
- [x] ربط RFQ بكتالوج الأسعار
- [x] توجيه أوامر الشراء للمدير العام عند تجاوز الحد
- [x] مزامنة الكميات المستلمة مع نظام العمائر
- [x] إضافة عدة مواد دفعة واحدة في نظام العمائر
- [x] حساب كمية البلاط بالمتر المربع

### تحسينات تقنية
- [x] إصلاح تحذيرات React hooks (exhaustive-deps)
- [x] تحسين رسائل الخطأ للعربية
- [x] إضافة Skeleton Loaders
- [x] إضافة Rate Limiting
- [x] إضافة Audit Logging
- [x] إضافة Validators مركزية

### تحسينات النسخ الاحتياطي
- [x] إضافة جداول نظام العمائر للنسخ الاحتياطي
- [x] إضافة جداول RFQ للنسخ الاحتياطي

---

## 📝 ملاحظات للمطور القادم

### 1. قبل أي تعديل
```
- اقرأ هذا الملف كاملاً
- تحقق من /app/memory/PRD.md
- راجع آخر test_reports في /app/test_reports/
```

### 2. عند إضافة جدول جديد
```
1. أضفه في database/models.py
2. أضفه في database/__init__.py (import و __all__)
3. أضفه في v2_system_routes.py للنسخ الاحتياطي
```

### 3. عند تعديل API
```
- استخدم الـ prefix /api/v2/
- أضف التحقق من الصلاحيات
- استخدم validators.py للتحقق من المدخلات
```

### 4. اختبار التغييرات
```bash
# Backend
tail -n 50 /var/log/supervisor/backend.err.log

# Frontend
cd /app/frontend && npm run build

# API Test
curl -X POST "$API_URL/api/v2/auth/login" ...
```

### 5. الأخطاء الشائعة في الكود
```python
# ❌ خطأ: إرجاع ObjectId من MongoDB
return {"_id": doc["_id"]}

# ✅ صحيح:
return {"id": str(doc["_id"])}

# ❌ خطأ: datetime بدون timezone
datetime.utcnow()

# ✅ صحيح:
datetime.now(timezone.utc)
```

---

## 🔐 بيانات الاختبار

### المستخدمين
| البريد | الدور | كلمة المرور |
|--------|-------|-------------|
| notofall@gmail.com | procurement_manager | 123456 |
| admin@system.com | system_admin | 123456 |
| Abdullah@test.com | general_manager | 123456 |
| abonaif@test.com | supervisor | 123456 |

### URLs
```
Production: http://13.235.247.19
Preview: https://buildapp-repair.preview.emergentagent.com
API Base: /api/v2/
```

---

## 📊 سير العمل (Workflows)

### سير طلب المواد
```
1. المشرف ينشئ طلب → status: draft
2. المشرف يرسل للمهندس → status: pending
3. المهندس يعتمد → status: approved_by_engineer
4. مدير المشتريات يصدر أمر شراء
   - إذا المبلغ > حد الموافقة → pending_gm_approval
   - إذا المبلغ <= حد الموافقة → pending_approval
5. (اختياري) المدير العام يعتمد
6. طباعة أمر الشراء → status: approved
7. استلام المواد → delivery_records
```

### سير طلب عرض السعر (RFQ)
```
1. إنشاء طلب عرض سعر → status: draft
2. إرسال للموردين → status: sent
3. استلام العروض من الموردين
4. مقارنة العروض واختيار الأفضل
5. تحويل لأمر شراء
```

### سير حساب كميات العمائر
```
1. إنشاء مشروع
2. إضافة الأدوار (floors) مع المساحات
3. إضافة مواد المساحة (area materials) مع المعامل
4. المزامنة → حساب الكميات المطلوبة
5. تتبع التوريد ← المستلم من أوامر الشراء
```

---

## 🛠️ أوامر مفيدة

### تشغيل محلي
```bash
# Backend
cd /app/backend
uvicorn server:app --reload --port 8001

# Frontend
cd /app/frontend
npm start
```

### Supervisor (في البيئة التطويرية)
```bash
sudo supervisorctl status
sudo supervisorctl restart backend
sudo supervisorctl restart frontend
```

### PostgreSQL
```bash
sudo -u postgres psql
\l                    # قائمة قواعد البيانات
\c talabat_db         # الاتصال بقاعدة البيانات
\dt                   # قائمة الجداول
\d users              # وصف جدول
```

---

**آخر تحديث:** 31 يناير 2026
**المؤلف:** Emergent AI Agent
